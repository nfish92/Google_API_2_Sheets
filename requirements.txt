# requirements.txt
# Python dependencies needed to run your Flask app

Flask                   # Main Python web framework
Flask-Dance             # Google OAuth support for Flask
gspread                 # Google Sheets API client
oauth2client            # Handles Google service account credentials
python-dotenv           # Loads environment variables from .env
flask-sslify            # Redirects HTTP to HTTPS (for Render deployment)
